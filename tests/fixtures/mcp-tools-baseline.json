{
  "tools": [
    {
      "name": "get_garmin_activities",
      "description": "Fetch recent Garmin activities with compact or full detail.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          },
          "offset": {
            "type": "integer",
            "minimum": 0
          },
          "detail": {
            "type": "string",
            "enum": [
              "compact",
              "full"
            ]
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_activity_splits",
      "description": "Read activity split/lap details from Garmin; read-only and never changes the activity.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "activityId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 128,
            "description": "Garmin activity ID returned by get_garmin_activities."
          }
        },
        "required": [
          "activityId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_activity_hr_zones",
      "description": "Read activity heart-rate zone details from Garmin; read-only and never changes the activity.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "activityId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 128,
            "description": "Garmin activity ID returned by get_garmin_activities."
          }
        },
        "required": [
          "activityId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_activity_polyline",
      "description": "Read the activity route polyline from Garmin; read-only and never changes the activity.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "activityId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 128,
            "description": "Garmin activity ID returned by get_garmin_activities."
          }
        },
        "required": [
          "activityId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_activity_weather",
      "description": "Read activity weather details from Garmin; read-only and never changes the activity.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "activityId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 128,
            "description": "Garmin activity ID returned by get_garmin_activities."
          }
        },
        "required": [
          "activityId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_daily_summary_chart",
      "description": "Read daily wellness summary chart data for a local date range; read-only.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "startDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Start date in YYYY-MM-DD format (default: today)"
          },
          "endDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Inclusive end date in YYYY-MM-DD format (maximum 30 days)"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_daily_intensity_minutes",
      "description": "Read daily intensity-minute data for a local date range; read-only.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "startDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Start date in YYYY-MM-DD format (default: today)"
          },
          "endDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Inclusive end date in YYYY-MM-DD format (maximum 30 days)"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_daily_movement",
      "description": "Read daily movement data for a local date range; missing days remain explicit; read-only.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "startDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Start date in YYYY-MM-DD format (default: today)"
          },
          "endDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Inclusive end date in YYYY-MM-DD format (maximum 30 days)"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_daily_respiration",
      "description": "Read daily respiration data for a local date range; read-only.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "startDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Start date in YYYY-MM-DD format (default: today)"
          },
          "endDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Inclusive end date in YYYY-MM-DD format (maximum 30 days)"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_sleep",
      "description": "Get sleep data for one date or an inclusive date range.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "startDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Start date in YYYY-MM-DD format (default: today)"
          },
          "endDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Inclusive end date in YYYY-MM-DD format (maximum 30 days)"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_steps",
      "description": "Get step totals for one date or an inclusive date range; goal and distance may be unavailable.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "startDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Start date in YYYY-MM-DD format (default: today)"
          },
          "endDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Inclusive end date in YYYY-MM-DD format (maximum 30 days)"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_heart_rate",
      "description": "Get heart-rate data for one date or an inclusive date range.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "startDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Start date in YYYY-MM-DD format (default: today)"
          },
          "endDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Inclusive end date in YYYY-MM-DD format (maximum 30 days)"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_weight",
      "description": "Get body-composition data for one date or an inclusive date range.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "startDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Start date in YYYY-MM-DD format (default: today)"
          },
          "endDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Inclusive end date in YYYY-MM-DD format (maximum 30 days)"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_workouts",
      "description": "Get workouts from the Garmin workout library.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          },
          "offset": {
            "type": "integer",
            "minimum": 0
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_profile",
      "description": "Get an allow-listed Garmin profile summary.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_body_battery",
      "description": "Read the current Garmin Body Battery summary; read-only.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_hrv",
      "description": "Read daily HRV data for a local date range; unavailable records remain explicit; read-only.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "startDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Start date in YYYY-MM-DD format (default: today)"
          },
          "endDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Inclusive end date in YYYY-MM-DD format (maximum 30 days)"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_sleep_stats",
      "description": "Read aggregate sleep statistics for an inclusive local date range; read-only.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "startDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Start date in YYYY-MM-DD format (default: today)"
          },
          "endDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Inclusive end date in YYYY-MM-DD format (maximum 30 days)"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_personal_records",
      "description": "Read Garmin personal records and history; read-only.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_goals",
      "description": "Read Garmin fitness goals by status; read-only.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "enum": [
              "active",
              "future",
              "past"
            ],
            "description": "Goal status; defaults to active."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_badges",
      "description": "Read earned Garmin badges and achievements; read-only.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_hydration",
      "description": "Read daily Garmin hydration data for a local date range; read-only.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "startDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Start date in YYYY-MM-DD format (default: today)"
          },
          "endDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Inclusive end date in YYYY-MM-DD format (maximum 30 days)"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_vo2max",
      "description": "Read Garmin VO2 max estimates for a local date range; read-only.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "startDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Start date in YYYY-MM-DD format (default: today)"
          },
          "endDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Inclusive end date in YYYY-MM-DD format (maximum 30 days)"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_fitness_stats",
      "description": "Read aggregated fitness statistics for an inclusive local date range; read-only.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "startDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
          },
          "endDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
          },
          "aggregation": {
            "type": "string",
            "enum": [
              "daily",
              "weekly",
              "monthly"
            ]
          },
          "metric": {
            "type": "string",
            "enum": [
              "duration",
              "distance",
              "calories"
            ]
          }
        },
        "required": [
          "startDate",
          "endDate"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_hr_zones_config",
      "description": "Read the configured Garmin heart-rate zone boundaries; read-only.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_power_zones",
      "description": "Read Garmin power-zone configuration for all sports; read-only.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_training_readiness",
      "description": "Read Garmin training-readiness data for a local date range; read-only.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "startDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Start date in YYYY-MM-DD format (default: today)"
          },
          "endDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Inclusive end date in YYYY-MM-DD format (maximum 30 days)"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_running_skill_advice",
      "description": "Explain 8 running workout types and the Hansons, Jack Daniels, Norwegian threshold, and polarized training philosophies. Set mode=personalized for any athlete-specific recommendation or plan. Personalized mode must collect goal, current performance and its basis, training background, availability, health/recovery constraints and warning-symptom status, plus load, quality-session, and intensity-guidance preferences before returning planning material; missing fields become questions and warning symptoms stop planning.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "mode": {
            "type": "string",
            "enum": [
              "explain",
              "personalized"
            ],
            "description": "Use explain for concepts only. Use personalized for any athlete-specific recommendation or plan."
          },
          "query": {
            "type": "string",
            "maxLength": 100,
            "description": "Optional workout type or philosophy keyword; omit for the full compact set."
          },
          "includeRecentActivities": {
            "type": "boolean",
            "description": "Fetch five recent runs only after personalized intake is complete; never a substitute for answers."
          },
          "language": {
            "type": "string",
            "enum": [
              "zh-CN",
              "en"
            ],
            "description": "Language for intake questions and coaching material."
          },
          "goal": {
            "type": "string",
            "minLength": 4,
            "maxLength": 500,
            "description": "Target distance or event, future ISO YYYY-MM-DD date, and completion or ideal/minimum time goal."
          },
          "currentPerformance": {
            "type": "string",
            "minLength": 4,
            "maxLength": 500,
            "description": "Representative result from the past two years, non-future ISO YYYY-MM-DD date, effort, and material conditions; explicitly state when none exists."
          },
          "performanceBasis": {
            "type": "string",
            "enum": [
              "recent_race",
              "time_trial",
              "no_recent_benchmark"
            ],
            "description": "Whether current ability comes from a recent race, time trial, or no recent benchmark."
          },
          "trainingBackground": {
            "type": "string",
            "minLength": 8,
            "maxLength": 1000,
            "description": "Running history, recent average/peak volume, frequency, long run, quality work, and interruptions."
          },
          "availability": {
            "type": "string",
            "minLength": 4,
            "maxLength": 750,
            "description": "Available days/time, rest and long-run days, terrain/facility limits, strength-training time, and whether double days are possible."
          },
          "healthConstraints": {
            "type": "string",
            "minLength": 2,
            "maxLength": 750,
            "description": "Current/past-year injury, relevant disease or medication, sleep, stress, and recovery; state none explicitly."
          },
          "hasWarningSymptoms": {
            "type": "boolean",
            "description": "True for current chest discomfort, abnormal breathlessness with mild activity, fainting/dizziness, or abnormal palpitations; true stops planning."
          },
          "trainingPreference": {
            "type": "string",
            "enum": [
              "steady",
              "hard_easy",
              "mixed"
            ],
            "description": "Preferred load pattern: steady/even, distinct hard/easy days, or mixed/no preference."
          },
          "maxQualitySessionsPerWeek": {
            "type": "integer",
            "minimum": 0,
            "maximum": 7,
            "description": "Maximum acceptable quality sessions per week; a ceiling, not a prescription."
          },
          "intensityGuidancePreference": {
            "type": "string",
            "enum": [
              "pace",
              "heart_rate",
              "rpe",
              "mixed"
            ],
            "description": "Preferred intensity guidance: pace, heart rate, perceived effort, or mixed."
          }
        },
        "required": [
          "mode"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "create_garmin_workout",
      "description": "Preview a user-specified structured workout first and create it only after explicit user confirmation. This execution tool does not generate a training plan. If the assistant derives a personalized workout, it must first complete get_running_skill_advice with mode=personalized; a directly specified workout may be encoded without coaching intake.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "maxLength": 80
          },
          "description": {
            "type": "string",
            "maxLength": 1024
          },
          "sport": {
            "type": "string",
            "enum": [
              "running",
              "cycling",
              "swimming",
              "strength"
            ]
          },
          "steps": {
            "type": "array",
            "items": {
              "anyOf": [
                {
                  "type": "object",
                  "properties": {
                    "type": {
                      "type": "string",
                      "enum": [
                        "warmup",
                        "interval",
                        "recovery",
                        "cooldown",
                        "rest"
                      ]
                    },
                    "description": {
                      "type": "string",
                      "minLength": 1,
                      "maxLength": 20
                    },
                    "endCondition": {
                      "type": "string",
                      "enum": [
                        "distance",
                        "time",
                        "lapButton"
                      ]
                    },
                    "endValue": {
                      "type": "number",
                      "exclusiveMinimum": 0,
                      "maximum": 1000000
                    },
                    "target": {
                      "type": "string",
                      "enum": [
                        "open",
                        "pace",
                        "heartRate"
                      ]
                    },
                    "paceFrom": {
                      "type": "string",
                      "pattern": "^\\d+:[0-5]\\d$"
                    },
                    "paceTo": {
                      "type": "string",
                      "pattern": "^\\d+:[0-5]\\d$"
                    },
                    "hrFrom": {
                      "type": "integer",
                      "minimum": 30,
                      "maximum": 250
                    },
                    "hrTo": {
                      "type": "integer",
                      "minimum": 30,
                      "maximum": 250
                    },
                    "hrZone": {
                      "type": "integer",
                      "minimum": 1,
                      "maximum": 5
                    }
                  },
                  "required": [
                    "type",
                    "endCondition"
                  ],
                  "additionalProperties": false
                },
                {
                  "type": "object",
                  "properties": {
                    "type": {
                      "type": "string",
                      "const": "repeat"
                    },
                    "iterations": {
                      "type": "integer",
                      "minimum": 1,
                      "maximum": 99
                    },
                    "steps": {
                      "type": "array",
                      "items": {
                        "$ref": "#/properties/steps/items/anyOf/0"
                      },
                      "minItems": 1,
                      "maxItems": 100
                    }
                  },
                  "required": [
                    "type",
                    "iterations",
                    "steps"
                  ],
                  "additionalProperties": false
                }
              ]
            },
            "minItems": 1,
            "maxItems": 100
          },
          "confirmed": {
            "type": "boolean",
            "description": "Set true only after the user explicitly approves the preview."
          },
          "confirmationId": {
            "type": "string",
            "maxLength": 256,
            "pattern": "^[^\\s:]+:\\d{1,9}$",
            "description": "One-time handle returned by the matching preview call, as <operationId>:<previewRevision>."
          },
          "idempotencyKey": {
            "type": "string",
            "minLength": 1,
            "maxLength": 128,
            "pattern": "^[A-Za-z0-9._:-]+$",
            "description": "Optional stable request label for this account. Reuse the same value to retrieve the durable operation instead of writing again; a different value never bypasses an in-flight or unknown write. Not a permission token."
          }
        },
        "required": [
          "name",
          "steps"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "create_garmin_workout_legacy",
      "description": "Preview and, after explicit confirmation, create a workout from the old Garmin DTO shape. The DTO is normalized into the new workout schema and uses the same journaled writer; this tool never sends the old raw payload directly.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "workout": {
            "type": "object",
            "additionalProperties": {},
            "description": "Subset of the old Garmin workout DTO."
          },
          "confirmed": {
            "type": "boolean",
            "description": "Set true only after the returned preview is approved."
          },
          "confirmationId": {
            "type": "string",
            "maxLength": 256,
            "pattern": "^[^\\s:]+:\\d{1,9}$",
            "description": "One-time handle returned by the matching preview call, as <operationId>:<previewRevision>."
          },
          "idempotencyKey": {
            "type": "string",
            "minLength": 1,
            "maxLength": 128,
            "pattern": "^[A-Za-z0-9._:-]+$",
            "description": "Optional stable request label for this account. Reuse the same value to retrieve the durable operation instead of writing again; a different value never bypasses an in-flight or unknown write. Not a permission token."
          }
        },
        "required": [
          "workout"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "schedule_garmin_workout",
      "description": "Preview then schedule one existing Garmin workout-library entry on a local calendar date. A workout may repeat on different dates. A repeated workout/date is skipped when Garmin already has it, and is blocked when a previous write for the same workout and date has an unknown outcome, so the same target is never written twice. The write is non-idempotent: after a timeout, query the operation and reconcile before retrying.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "workoutId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 128,
            "description": "Existing Garmin workout-library ID."
          },
          "date": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Local Garmin Calendar date in YYYY-MM-DD format"
          },
          "timezone": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100,
            "description": "IANA timezone used to interpret date, e.g. Asia/Shanghai; defaults to the MCP host timezone"
          },
          "confirmed": {
            "type": "boolean",
            "description": "Set true only after the user explicitly approves the returned preview."
          },
          "confirmationId": {
            "type": "string",
            "maxLength": 256,
            "pattern": "^[^\\s:]+:\\d{1,9}$",
            "description": "One-time handle returned by the matching preview call, as <operationId>:<previewRevision>."
          },
          "idempotencyKey": {
            "type": "string",
            "minLength": 1,
            "maxLength": 128,
            "pattern": "^[A-Za-z0-9._:-]+$",
            "description": "Optional stable request label for this account. Reuse the same value to retrieve the durable operation instead of writing again; a different value never bypasses an in-flight or unknown write. Not a permission token."
          },
          "duplicatePolicy": {
            "type": "string",
            "enum": [
              "skip",
              "error"
            ],
            "description": "What to do when a fresh calendar read shows this target is already scheduled. 'skip' (default) reports it as skipped without writing; 'error' reports it as duplicate_existing and still writes nothing. Neither option deletes or overwrites an existing entry. The value is part of the confirmed request, so it must be the same on the preview and confirm calls."
          }
        },
        "required": [
          "workoutId",
          "date"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "batch_schedule_garmin_workouts",
      "description": "Preview then schedule 1-100 existing Garmin workout-library entries across future days or weeks. Each entry is committed separately and reports its own status (succeeded, skipped, failed, not_attempted or unknown). A timeout is reported as unknown and is never re-sent; entries that share an unknown or in-flight target are blocked. Do not represent rest days: omit them instead of creating a workout.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "schedules": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "workoutId": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 128
                },
                "date": {
                  "type": "string",
                  "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
                  "description": "Local Garmin Calendar date in YYYY-MM-DD format"
                }
              },
              "required": [
                "workoutId",
                "date"
              ],
              "additionalProperties": false
            },
            "minItems": 1,
            "maxItems": 100
          },
          "timezone": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100,
            "description": "IANA timezone used to interpret date, e.g. Asia/Shanghai; defaults to the MCP host timezone"
          },
          "confirmed": {
            "type": "boolean",
            "description": "Set true only after the user explicitly approves the returned preview."
          },
          "confirmationId": {
            "type": "string",
            "maxLength": 256,
            "pattern": "^[^\\s:]+:\\d{1,9}$",
            "description": "One-time handle returned by the matching preview call, as <operationId>:<previewRevision>."
          },
          "idempotencyKey": {
            "type": "string",
            "minLength": 1,
            "maxLength": 128,
            "pattern": "^[A-Za-z0-9._:-]+$",
            "description": "Optional stable request label for this account. Reuse the same value to retrieve the durable operation instead of writing again; a different value never bypasses an in-flight or unknown write. Not a permission token."
          },
          "duplicatePolicy": {
            "type": "string",
            "enum": [
              "skip",
              "error"
            ],
            "description": "What to do when a fresh calendar read shows this target is already scheduled. 'skip' (default) reports it as skipped without writing; 'error' reports it as duplicate_existing and still writes nothing. Neither option deletes or overwrites an existing entry. The value is part of the confirmed request, so it must be the same on the preview and confirm calls."
          }
        },
        "required": [
          "schedules"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "create_and_schedule_garmin_workout",
      "description": "Preview then create one structured Garmin workout and schedule it on a local calendar date in one confirmed operation. If creation succeeds but scheduling fails, the result reports the partial outcome and must not be blindly retried. Both phases are journaled: a repeated request with the same idempotencyKey returns the stored operation instead of creating another template or another calendar entry.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "workout": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "minLength": 1,
                "maxLength": 80
              },
              "description": {
                "type": "string",
                "maxLength": 1024
              },
              "sport": {
                "type": "string",
                "enum": [
                  "running",
                  "cycling",
                  "swimming",
                  "strength"
                ]
              },
              "steps": {
                "type": "array",
                "items": {
                  "anyOf": [
                    {
                      "type": "object",
                      "properties": {
                        "type": {
                          "type": "string",
                          "enum": [
                            "warmup",
                            "interval",
                            "recovery",
                            "cooldown",
                            "rest"
                          ]
                        },
                        "description": {
                          "type": "string",
                          "minLength": 1,
                          "maxLength": 20
                        },
                        "endCondition": {
                          "type": "string",
                          "enum": [
                            "distance",
                            "time",
                            "lapButton"
                          ]
                        },
                        "endValue": {
                          "type": "number",
                          "exclusiveMinimum": 0,
                          "maximum": 1000000
                        },
                        "target": {
                          "type": "string",
                          "enum": [
                            "open",
                            "pace",
                            "heartRate"
                          ]
                        },
                        "paceFrom": {
                          "type": "string",
                          "pattern": "^\\d+:[0-5]\\d$"
                        },
                        "paceTo": {
                          "type": "string",
                          "pattern": "^\\d+:[0-5]\\d$"
                        },
                        "hrFrom": {
                          "type": "integer",
                          "minimum": 30,
                          "maximum": 250
                        },
                        "hrTo": {
                          "type": "integer",
                          "minimum": 30,
                          "maximum": 250
                        },
                        "hrZone": {
                          "type": "integer",
                          "minimum": 1,
                          "maximum": 5
                        }
                      },
                      "required": [
                        "type",
                        "endCondition"
                      ],
                      "additionalProperties": false
                    },
                    {
                      "type": "object",
                      "properties": {
                        "type": {
                          "type": "string",
                          "const": "repeat"
                        },
                        "iterations": {
                          "type": "integer",
                          "minimum": 1,
                          "maximum": 99
                        },
                        "steps": {
                          "type": "array",
                          "items": {
                            "$ref": "#/properties/workout/properties/steps/items/anyOf/0"
                          },
                          "minItems": 1,
                          "maxItems": 100
                        }
                      },
                      "required": [
                        "type",
                        "iterations",
                        "steps"
                      ],
                      "additionalProperties": false
                    }
                  ]
                },
                "minItems": 1,
                "maxItems": 100
              }
            },
            "required": [
              "name",
              "steps"
            ],
            "additionalProperties": false
          },
          "date": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Local Garmin Calendar date in YYYY-MM-DD format"
          },
          "timezone": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100,
            "description": "IANA timezone used to interpret date, e.g. Asia/Shanghai; defaults to the MCP host timezone"
          },
          "confirmed": {
            "type": "boolean",
            "description": "Set true only after the user explicitly approves the returned preview."
          },
          "confirmationId": {
            "type": "string",
            "maxLength": 256,
            "pattern": "^[^\\s:]+:\\d{1,9}$",
            "description": "One-time handle returned by the matching preview call, as <operationId>:<previewRevision>."
          },
          "idempotencyKey": {
            "type": "string",
            "minLength": 1,
            "maxLength": 128,
            "pattern": "^[A-Za-z0-9._:-]+$",
            "description": "Optional stable request label for this account. Reuse the same value to retrieve the durable operation instead of writing again; a different value never bypasses an in-flight or unknown write. Not a permission token."
          }
        },
        "required": [
          "workout",
          "date"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "unschedule_garmin_workout",
      "description": "Preview then remove one Garmin Calendar entry by the workoutScheduleId returned from a prior schedule operation. This removes the calendar entry, not the reusable workout-library template. Removal is journaled: a repeated request with the same idempotencyKey returns the stored operation instead of deleting again.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "workoutScheduleId": {
            "type": "string",
            "minLength": 1,
            "maxLength": 128,
            "description": "Garmin Calendar entry ID returned as workoutScheduleId by a schedule operation."
          },
          "confirmed": {
            "type": "boolean",
            "description": "Set true only after the user explicitly approves the returned preview."
          },
          "confirmationId": {
            "type": "string",
            "maxLength": 256,
            "pattern": "^[^\\s:]+:\\d{1,9}$",
            "description": "One-time handle returned by the matching preview call, as <operationId>:<previewRevision>."
          },
          "idempotencyKey": {
            "type": "string",
            "minLength": 1,
            "maxLength": 128,
            "pattern": "^[A-Za-z0-9._:-]+$",
            "description": "Optional stable request label for this account. Reuse the same value to retrieve the durable operation instead of writing again; a different value never bypasses an in-flight or unknown write. Not a permission token."
          }
        },
        "required": [
          "workoutScheduleId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "download_garmin_activity_fit",
      "description": "Download one Garmin activity as a FIT file. GARMIN_FIT_DOWNLOAD_DIR must explicitly select a trusted local parent directory; files are isolated under GARMIN_FIT_<region>_<account-email>. Returns non-sensitive metadata without the local path or binary content.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "activityId": {
            "type": "integer",
            "minimum": 1,
            "maximum": 9007199254740991,
            "description": "Positive Garmin activity ID returned by get_garmin_activities."
          }
        },
        "required": [
          "activityId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_calendar",
      "description": "Read the Garmin Calendar for an inclusive local-date range of at most 366 days. Returns a CalendarSnapshot: the entries it actually observed, whether the read covered the whole range, and the sub-ranges it could not read. An incomplete read is never an empty calendar, and no range read of any kind proves that a specific write attempt never reached Garmin — that question is answered by reconcile_garmin_write_operation against the local journal. Purely read-only: this call schedules, creates and deletes nothing.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "startDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Inclusive first calendar day in YYYY-MM-DD format."
          },
          "endDate": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "Inclusive last calendar day in YYYY-MM-DD format; at most 366 days after startDate."
          },
          "timezone": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100,
            "description": "IANA timezone label echoed back on the snapshot, e.g. Asia/Shanghai. Defaults to the MCP host timezone. It never shifts a date: the verified provider query carries no timezone parameter."
          }
        },
        "required": [
          "startDate",
          "endDate"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "get_garmin_write_operation",
      "description": "Read the local account-scoped write journal without any network access. Supply either an operationId or an idempotencyKey to look up a single operation; those two are mutually exclusive and cannot be combined with limit or cursor. Supplying neither pages through recent operations for the active account (newest first, limit default 20, maximum 100) and returns a nextCursor for the following page. The cursor is an opaque, bounded local token, never a path, and it is valid only while the journal is unchanged: if a record moved, the response is `staleCursor:true` with no `operations` key at all, so an empty page can never be mistaken for an empty journal. A record that does not exist — including one belonging to another account — is reported uniformly as OPERATION_NOT_FOUND. The returned records are redacted: the raw idempotency key, the request payload, and the account key are never echoed back. Each record carries its own roll-up (status, canResume, manualReviewRequired, nextAction) so you do not have to reconstruct the verdict from step bookkeeping; use that nextAction to drive reconcile_garmin_write_operation or resume_garmin_write_operation. Does not require login and never contacts Garmin.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "operationId": {
            "type": "string",
            "format": "uuid",
            "description": "Operation ID returned by a previous schedule_garmin_workout, batch_schedule_garmin_workouts, create_garmin_workout, create_and_schedule_garmin_workout or unschedule_garmin_workout call."
          },
          "idempotencyKey": {
            "type": "string",
            "minLength": 1,
            "maxLength": 128,
            "pattern": "^[A-Za-z0-9._:-]+$",
            "description": "Caller-supplied stable request label; only its hash is matched against the journal."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "description": "Page size for the listing mode (default 20, maximum 100). Rejected when operationId or idempotencyKey is used."
          },
          "cursor": {
            "type": "string",
            "pattern": "^[A-Za-z0-9_-]{1,2048}$",
            "description": "Opaque nextCursor from a previous listing: 1-2048 characters from A-Z a-z 0-9 _ - only, so it can never be interpreted as a path. Rejected when operationId or idempotencyKey is used. It expires as soon as the journal changes."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "reconcile_garmin_write_operation",
      "description": "Re-read Garmin to resolve what a local write operation is still unsure about, and record the trusted observations in the local recovery journal. It sends no schedule, create or unschedule request and deletes nothing anywhere: an attempt with an unknown outcome stays unknown until a real receipt exists, and an entry observed to be absent is reported as absent without any past result being rewritten. Because it updates local state it is not advertised as read-only, but it never modifies Garmin. Returns the observations it took and the next action to take.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "operationId": {
            "type": "string",
            "format": "uuid",
            "description": "Operation ID returned by a previous schedule_garmin_workout, batch_schedule_garmin_workouts, create_garmin_workout, create_and_schedule_garmin_workout or unschedule_garmin_workout call."
          }
        },
        "required": [
          "operationId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    },
    {
      "name": "resume_garmin_write_operation",
      "description": "Derive the safe remaining steps of a journaled write operation and, once the caller approves the preview, dispatch them. There is no payload parameter: dates, workout ids and template definitions all come from the journal record, so a resume can never move a write to a different day or swap the template — that would be a new request, not a recovery. Only steps proven never to have applied are armed; an unknown outcome is never re-sent and cannot be cleared from here. The dispatch is non-idempotent: re-query the operation for the durable result instead of calling it again.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "operationId": {
            "type": "string",
            "format": "uuid",
            "description": "Operation ID returned by a previous schedule_garmin_workout, batch_schedule_garmin_workouts, create_garmin_workout, create_and_schedule_garmin_workout or unschedule_garmin_workout call."
          },
          "confirmed": {
            "type": "boolean",
            "description": "Set true only after the user explicitly approves the returned preview."
          },
          "confirmationId": {
            "type": "string",
            "maxLength": 256,
            "pattern": "^[^\\s:]+:\\d{1,9}$",
            "description": "One-time handle returned by the matching preview call, as <operationId>:<previewRevision>."
          }
        },
        "required": [
          "operationId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      },
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": true
      },
      "execution": {
        "taskSupport": "forbidden"
      }
    }
  ]
}
